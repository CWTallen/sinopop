server {
    listen 8080;

    location /actuator/health {
        default_type application/json;
        return 200 '{"status":"UP","placeholder":true}';
    }

    location / {
        default_type text/plain;
        return 200 "sinopop-app placeholder image - replace with real Spring Boot build\n";
    }
}
